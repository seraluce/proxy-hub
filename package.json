{
  "name": "gh-proxy",
  "version": "2.0.0",
  "description": "GitHub Release、Archive 及项目文件的加速项目，基于 Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
